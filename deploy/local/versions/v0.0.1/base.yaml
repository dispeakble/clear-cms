version: "3.2"
services:

  redis:
    container_name: redis
    image: "redis:alpine"
    restart: always
    command: "redis-server --requirepass '${REDIS_DEFAULT_PASSWORD}'"
    ports:
      - "6379:6379"
    volumes:
      - ${PWD}/volumes/redis:/usr/local/etc/redis
    environment:
      - REDIS_REPLICATION_MODE=master
    networks:
      node_net:
        ipv4_address: 172.28.1.1

  postgres:
    container_name: postgres
    image: "postgres:11.14-alpine" # use latest official postgres version
    healthcheck:
      test: [ "CMD", "pg_isready", "-q", "-d", "postgres", "-U", "root" ]
      timeout: 45s
      interval: 10s
      retries: 10
    restart: always
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: ${POSTGRES_DEFAULT_USER}
      POSTGRES_DB: 'postgres'
      POSTGRES_PASSWORD: ${POSTGRES_DEFAULT_PASSWORD}
    volumes:
      - postgresdata:/var/lib/postgresql
    networks:
      node_net:
        ipv4_address: 172.28.1.2

  pgadmin:
    container_name: pgadmin
    image: dpage/pgadmin4
    restart: always
    healthcheck:
      test: |
        apk add --no-cache sqlite
        sqlite3 /var/lib/pgadmin/pgadmin4.db 'INSERT OR IGNORE INTO server (id, user_id, servergroup_id, name, host, port, maintenance_db, username, password, ssl_mode, use_ssh_tunnel, tunnel_port, tunnel_authentication, connect_timeout) VALUES (10, 1, 1, "chatbot_prog_1", "postgres", 5432, "dbname", "dbuser", NULL, "prefer", 0, 22, 0, 0);' '.exit'
        exit 1
      interval: 0s
      timeout: 10s
      retries: 1
    depends_on:
      - postgres
    environment:
      PGADMIN_DEFAULT_EMAIL: ${PGADMIN_DEFAULT_EMAIL}
      PGADMIN_DEFAULT_PASSWORD: ${PGADMIN_DEFAULT_PASSWORD}
    volumes:
      - ${PWD}/../versions/config/pgadmin4/servers.json:/pgadmin4/servers.json
      - pgadmindata:/var/lib
      - ${PWD}/../versions/config/pgadmin4/pgpass.txt:/pgpass.txt
    ports:
      - "5050:80"
    networks:
      node_net:
        ipv4_address: 172.28.1.3

  cms-hub:
    container_name: cms-hub
    image: "${DOCKER_REGISTRY_USERNAME}/crm-hub:${CMS_HUB_VERSION}"
    restart: always
    depends_on:
      - postgres
      - redis
    environment:
      - app=${APP_NAME}
      - app_version=${APP_VERSION}
      - admin_fname=${ADMIN_FNAME}
      - admin_lname=${ADMIN_LNAME}
      - admin_email=${DEFAULT_EMAIL}
      - admin_password=${DEFAULT_PASSWORD}
      - website_name=${WEBSITE_NAME}
      - website_domain=${WEBSITE_DOMAIN}
      - website_email=${WEBSITE_EMAIL}
      - redis_server=172.28.1.1
      - redis_port=6379
      - redis_password=${REDIS_DEFAULT_PASSWORD}
    networks:
      node_net:
        ipv4_address: 172.28.1.10
    tty: true

  cms-db:
    container_name: cms-db
    image: "${DOCKER_REGISTRY_USERNAME}/crm-db:${CMS_DB_VERSION}"
    restart: always
    depends_on:
      - postgres
      - redis
      - cms-hub
    environment:
      - redis_server=172.28.1.1
      - redis_port=6379
      - redis_password=${REDIS_DEFAULT_PASSWORD}
      - pg_host=172.28.1.2
      - pg_port=5432
      - pg_user=${POSTGRES_DEFAULT_USER}
      - pg_password=${POSTGRES_DEFAULT_PASSWORD}
      - pg_db=${POSTGRES_DEFAULT_DB}
      - pg_connections=${POSTGRES_DEFAULT_CONNECTIONS}
      - app=${APP_NAME}
    networks:
      node_net:
        ipv4_address: 172.28.1.11
    tty: true

  cms-bucket:
    container_name: cms-bucket
    image: "${DOCKER_REGISTRY_USERNAME}/crm-bucket:${CMS_BUCKET_VERSION}"
    restart: always
    depends_on:
      - postgres
      - redis
      - cms-hub
    volumes:
      - cms-bucket:/usr/src/var
    environment:
      - app=${APP_NAME}
      - redis_server=172.28.1.1
      - redis_port=6379
      - redis_password=${REDIS_DEFAULT_PASSWORD}
    networks:
      node_net:
        ipv4_address: 172.28.1.12
    tty: true

  cms-admin-proxy:
    container_name: cms-admin-proxy
    image: "${DOCKER_REGISTRY_USERNAME}/crm-proxy:${CMS_ADMIN_PROXY_VERSION}"
    restart: always
    depends_on:
      - redis
      - cms-hub
      - cms-db
      - cms-bucket
    environment:
      - app=${APP_NAME}
      - redis_server=172.28.1.1
      - redis_port=6379
      - redis_password=${REDIS_DEFAULT_PASSWORD}
      - backend_port=9696
      - session_secret=1qaz
    ports:
      - "127.0.0.1:9696:9696"
    networks:
      node_net:
        ipv4_address: 172.28.1.13
    tty: true

  cms-public-proxy:
    container_name: cms-public-proxy
    image: "${DOCKER_REGISTRY_USERNAME}/crm-frontend-proxy:${CMS_FRONTEND_PROXY_VERSION}"
    restart: always
    depends_on:
      - redis
      - cms-hub
      - cms-db
      - cms-bucket
    environment:
      - app=${APP_NAME}
      - redis_server=172.28.1.1
      - redis_port=6379
      - redis_password=${REDIS_DEFAULT_PASSWORD}
      - backend_port=9797
      - session_secret=1qaz
    ports:
      - "127.0.0.1:9797:9797"
    networks:
      node_net:
        ipv4_address: 172.28.1.14
    tty: true

  cms-frontend:
    container_name: cms-frontend
    image: "${DOCKER_REGISTRY_USERNAME}/crm-frontend-api:${CMS_FRONTEND_VERSION}"
    restart: always
    depends_on:
      - postgres
      - redis
      - cms-hub
      - cms-db
    environment:
      - app=${APP_NAME}
      - website_domain=${WEBSITE_DOMAIN}
      - tpl_ver=${TPL_VER}
      - backend_port=9898
      - redis_server=172.28.1.1
      - redis_port=6379
      - redis_password=${REDIS_DEFAULT_PASSWORD}
      - NODE_ENV=production
      - CMS_JWT_EXP_H=${CMS_JWT_EXP_H}
      - recaptcha_public_key=${recaptcha_public_key}
      - recaptcha_private_key=${recaptcha_private_key}
    ports:
      - "127.0.0.1:9898:9898"
    networks:
      node_net:
        ipv4_address: 172.28.1.15
    tty: true

  cms-system:
    container_name: cms-system
    image: "${DOCKER_REGISTRY_USERNAME}/crm-system:${CMS_ADMIN_SYSTEM_VERSION}"
    restart: always
    depends_on:
      - postgres
      - redis
      - cms-hub
      - cms-db
    environment:
      - app=${APP_NAME}
      - redis_server=172.28.1.1
      - redis_port=6379
      - redis_password=${REDIS_DEFAULT_PASSWORD}
      - backend_port=9696
    networks:
      node_net:
        ipv4_address: 172.28.1.16
    tty: true
networks:
  node_net:
    ipam:
      driver: default
      config:
        - subnet: 172.28.0.0/16
volumes:
  redisdata:
    driver: local
    driver_opts:
      type: 'none'
      o: 'bind'
      device: '${PWD}/volumes/redis'
  postgresdata:
    driver: local
    driver_opts:
      type: 'none'
      o: 'bind'
      device: '${PWD}/volumes/postgres'
  pgadmindata:
    driver: local
    driver_opts:
      type: 'none'
      o: 'bind'
      device: '${PWD}/volumes/pgadmin'
  cms-bucket:
    driver: local
    driver_opts:
      type: 'none'
      o: 'bind'
      device: '${PWD}/../../../plugins/dosido/bucket/var'
