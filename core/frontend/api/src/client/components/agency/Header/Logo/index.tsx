import styled from 'styled-components'

const Logo = () => {
    return (
        <LogoWrapper>
            Logo
        </LogoWrapper>
    )
}

const LogoWrapper = styled.div`
    color:#fff;
    font-size: 24px;
`

export default Logo